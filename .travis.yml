language: objective-c

compiler:
  - clang

before_script:
  - git clone --depth=50 --branch=master git://github.com/takram-design-engineering/takram-math.git ../takram-math
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make test
